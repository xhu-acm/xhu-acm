## L2-3 龙龙送外卖

龙龙是“饱了呀”外卖软件的注册骑手，负责送帕特小区的外卖。帕特小区的构造非常特别，都是双向道路且没有构成环 —— 你可以简单地认为小区的路构成了一棵树，根结点是外卖站，树上的结点就是要送餐的地址。

每到中午 12 点，帕特小区就进入了点餐高峰。一开始，只有一两个地方点外卖，龙龙简单就送好了；但随着大数据的分析，龙龙被派了更多的单子，也就送得越来越累……

看着一大堆订单，龙龙想知道，从外卖站出发，访问所有点了外卖的地方至少一次（这样才能把外卖送到）所需的最短路程的距离到底是多少？每次新增一个点外卖的地址，他就想估算一遍整体工作量，这样他就可以搞明白新增一个地址给他带来了多少负担。

### 输入格式:

输入第一行是两个数 N 和 M (2≤N≤105, 1≤M≤105)，分别对应树上节点的个数（包括外卖站），以及新增的送餐地址的个数。

接下来首先是一行 N 个数，第 i 个数表示第 i 个点的双亲节点的编号。节点编号从 1 到 N，外卖站的双亲编号定义为 −1。

接下来有 M 行，每行给出一个新增的送餐地点的编号 Xi​。保证送餐地点中不会有外卖站，但地点有可能会重复。

为了方便计算，我们可以假设龙龙一开始一个地址的外卖都不用送，两个相邻的地点之间的路径长度统一设为 1，且从外卖站出发可以访问到所有地点。

注意：所有送餐地址可以按任意顺序访问，_**且完成送餐后无需返回外卖站**_。

### 输出格式:

对于每个新增的地点，在一行内输出题目需要求的最短路程的距离。

### 输入样例:

```
7 4
-1 1 1 1 2 2 3
5
6
2
4
```

### 输出样例:

```
2
4
4
6
```

# 题解

外卖站不一定是第一个输入

* **如果必须返回外卖站**：那么每增加一个送餐点，从根节点（外卖站）到该点的路径必然要被走两遍（一来一回）。此时总距离就是 $2 \times \text{树上被激活的边数}$。

* **无需返回外卖站**：为了使总路程最短，龙龙应该把**距离外卖站最远的那个送餐点**留到最后去送，这样他送完之后就不需要再走回头路了。

* **推导公式**：最短总距离 = $2 \times \text{当前被激活的总边数} - \text{当前所有要送餐的节点中的最大深度}$。

### 代码

```c++
#if 1
#include<bits/stdc++.h>
#define int long long
using namespace std;
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr); cout.tie(nullptr);
    int  n, m;
    cin >> n >> m;
    vector<int> nod(n + 1);
    int waima = 0;
    for (int i = 1; i <= n; i++)
    {
        cin >> nod[i];
        if (nod[i] == -1)
        {
            waima = i;
        }
    }
    set<int> has;
    vector<int>deep(n + 2, 0);
    has.insert(waima);
    int max_deep = 0;
    for (int i = 0; i < m; i++)
    {
        int add;
        cin >> add;       
        int temp_deep = 0;
        vector<int> record;
        if (has.count(add) == 0)
        {
            while (add!=-1)
            {

                if (has.count(add) == 0)
                {
                    temp_deep++;
                    has.insert(add);
                    record.push_back(add);
                    add = nod[add];
                }
                else
                {
                    break;
                }

            }
            temp_deep += deep[add];
            if (temp_deep > max_deep)
            {
                max_deep = temp_deep;
            }
            //while 循环结束时，add为路线遇到的第一个已经走过的节点

            for (auto j : record)
            {
                deep[j] = temp_deep;
                temp_deep--;
            }
        }
       /* cout << "max_deep=" << max_deep << endl;
        cout << "has.size()=" << has.size() << endl;
        cout << "ans=";*/
        cout << (has.size() - 1) * 2 - max_deep; 
        //cout << "------------------------" << endl;
        if (i != m - 1)
        {
            cout << endl;
        }
    }

    return 0;
}
#endif
```
